% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_study_data.R
\name{check_y_bounds}
\alias{check_y_bounds}
\title{Check that outcome variables are within [0, 1]}
\usage{
check_y_bounds(
  data,
  study_id = NULL,
  outcomes = NULL,
  prefix = "Y_",
  exclude = NULL
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{study_id}{Optional character scalar. If provided, a \code{study_id}
column is appended to the returned tibble.}

\item{outcomes}{Columns to check. Supply unquoted names or tidyselect
helpers (e.g., \code{starts_with("outcome_")}), or a character vector of
column names. If omitted, all columns starting with \code{prefix} are
used (excluding \code{"_missing"} and \code{"_s"} suffixes).}

\item{prefix}{Character string. Prefix used to auto-select outcome columns
when \code{outcomes} is omitted (default: \code{"Y_"}).}

\item{exclude}{Additional columns to drop from the selection. Supply
unquoted names or tidyselect helpers (e.g., \code{ends_with("_raw")}),
or a character vector of exact column names. Applied after
\code{outcomes} is resolved. \code{NULL} (the default) means no
additional exclusions.}
}
\value{
A tibble with columns \code{variable}, \code{min}, \code{max},
  \code{in_bounds}, and optionally \code{study_id}.
}
\description{
Summarises the min and max of each outcome variable and flags any that
fall outside [0, 1].
}
\details{
When \code{outcomes} is omitted, all columns whose names start with
\code{prefix} are selected, excluding any that end with \code{"_missing"}
or \code{"_s"} (standardised versions). Supply \code{outcomes} to override
this default, or \code{exclude} to drop additional columns from whatever
was selected.
}
\examples{
dat <- data.frame(Y_support = c(0, 0.5, 1), Y_oppose = c(0, 1.2, 0.8))
check_y_bounds(dat)
check_y_bounds(dat, study_id = "my_study")
check_y_bounds(dat, outcomes = "Y_support")

}
\seealso{
Other per-study checks:
\code{\link[=check_attrition]{check_attrition()}},
\code{\link[=check_attrition_lasso]{check_attrition_lasso()}},
\code{\link[=check_balance]{check_balance()}},
\code{\link[=check_covariate_missingness]{check_covariate_missingness()}},
\code{\link[=check_missingness_nona]{check_missingness_nona()}},
\code{\link[=check_smd]{check_smd()}}
}
\concept{per-study checks}
