% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_schema.R
\name{assert_schema}
\alias{assert_schema}
\title{Assert a cleaned data frame conforms to its schema}
\usage{
assert_schema(data, ..., study_id = NULL)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{...}{Passed to \code{check_schema}.}

\item{study_id}{Optional character scalar appended as a \code{study_id}
column, so results stack across studies.}
}
\value{
The \code{check_schema} report tibble, invisibly.
}
\description{
Runs \code{check_schema} and turns the result into a hard stop, for use at
the tail of a cleaning script (before \code{write_rds}) so a nonconforming
study fails loudly where it is built. Any failing \code{"error"} check stops;
any failing \code{"warn"} check emits a warning. Returns the report invisibly.
}
\examples{
dat <- data.frame(resp_id = 1:3, weights = 1, Z = c(0, 1, 0), Y = c(0, 1, 1))
assert_schema(dat)

}
\seealso{
Other schema assertions:
\code{\link[=assert_key_unique]{assert_key_unique()}},
\code{\link[=assert_no_labelled]{assert_no_labelled()}},
\code{\link[=check_schema]{check_schema()}}
}
\concept{schema assertions}
